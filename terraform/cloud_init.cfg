users:
  - name: ${user}
    groups: sudo
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    ssh_authorized_keys:
      - ${ssh_key}
packages:
  - python3
  - python3-pip
runcmd:
  - echo "Cloud Init has successfully configured the user and keys"